[package]
name = "noombat-chat"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "IMAP/SMTP transport proxy for Chatmail; deltachat-rpc-server fallback."

[dependencies]
noombat-core = { workspace = true }
