[package]
name = "noombat-events"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Event CRUD, RSVP, and calendar feeds."

[dependencies]
noombat-core = { workspace = true }
