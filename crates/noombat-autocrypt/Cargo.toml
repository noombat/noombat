[package]
name = "noombat-autocrypt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Autocrypt Level 1 state machine and rPGP integration; compiles to wasm32-unknown-unknown."

[dependencies]
noombat-core = { workspace = true }
